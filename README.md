# cWGAN package (refactor-only)

This package exposes the model API only.

## Public API
- `cwgan.Generator`
- `cwgan.Discriminator`
- `cwgan.set_seed`, `cwgan.get_device`, `cwgan.load_json`, `cwgan.save_json`
