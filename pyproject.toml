[project]
name = "cwgan"
version = "0.1.0"
description = "cWGAN model package (refactor-only submission)"
readme = "README.md"
requires-python = ">=3.9"

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
